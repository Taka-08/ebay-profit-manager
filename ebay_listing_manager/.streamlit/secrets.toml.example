# The deployed apps use the repository-root .streamlit settings.
# Keep this file only as a reference when starting this subdirectory directly.

[auth]
REQUIRE_AUTH = true
APP_USERNAME = "admin"
APP_PASSWORD = "replace-with-a-long-random-password"
# APP_PASSWORD_HASH = "pbkdf2_sha256$600000$...$..."

[database]
TURSO_DATABASE_URL = "libsql://replace-with-your-database.turso.io"
TURSO_AUTH_TOKEN = "replace-with-your-database-token"
